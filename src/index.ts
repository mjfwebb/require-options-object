import { rule } from "./require-options-object.js";

const plugin = {
  meta: {
    name: "require-options-object",
  },
  rules: {
    "require-options-object": rule,
  },
};

export default plugin;
